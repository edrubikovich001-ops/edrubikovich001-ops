services:
  - type: worker
    name: telegram-bot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python run_local.py"
    envVars:
      - key: BOT_TOKEN
        sync: false
